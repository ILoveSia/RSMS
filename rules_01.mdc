Always respond in Korean. Do not use emojis like ✅.

Do not remove a feature just because you were told to fix an error.

Do not seek ad-hoc solutions just to suppress immediate errors. Always aim for solutions that fulfill the given
Never forget to maintain clear separation of concerns and responsibilities between modules.

Generate a class structure that strictly follows the SOLID principles.

Ensure each class has a single responsibility, is closed for modification but open for extension, respects substitution
 rules, depends only on relevant interfaces, and inverts dependencies where applicable.

Always explain structural decisions in code comments or docstrings so other developers can understand why the
 structure was chosen, not just how it works.